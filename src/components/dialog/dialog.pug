dialog(class="dialog" id="dialog-id")
  .dialog__block 
    svg.dialog__icon(viewBox="0 0 29 28")
      use(href="./svg/floppy-icon.svg#floppy__icon")
    h2.dialog__title вставьте дискету, чтобы продолжить
  form.dialog__form(method="dialog")
    button(
      type="button"
      class="button button__accept"
      onclick="window['dialog-id'].close()"
      aria-controls="dialog-id"
    )
      span.button__text ок
